export const PATHS = {
  HOME: "/",
  LOGIN: "/login",
  REGISTER: "/register",
  PROFILE: "/profile",
  SCHEDULE: "/schedule",
  RESCHEDULE: "/reschedule",
  MANAGE: "/manage",
};

export const HIDE_NAVBAR_PATHS = [PATHS.LOGIN, PATHS.REGISTER];

export const PUBLIC_PATHS = [PATHS.HOME, PATHS.LOGIN, PATHS.REGISTER];

export const PRIVATE_PATHS = [];
